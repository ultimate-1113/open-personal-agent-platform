{
  "name": "@opap/conversation-agent",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@opap/contracts": "workspace:*"
  }
}
