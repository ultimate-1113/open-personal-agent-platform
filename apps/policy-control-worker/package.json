{
  "name": "@opap/policy-control-worker",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@opap/approval": "workspace:*",
    "@opap/contracts": "workspace:*",
    "@opap/identity": "workspace:*",
    "@opap/policy-engine": "workspace:*",
    "@opap/security": "workspace:*",
    "jose": "^6.2.9"
  }
}
