{
  "name": "@opap/github-gatekeeper",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opap/approval": "workspace:*",
    "@opap/github-connector": "workspace:*",
    "@opap/oauth-core": "workspace:*",
    "jose": "^6.2.9"
  }
}
